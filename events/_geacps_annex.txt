add_namespace = geacps_annex
country_event = {
	id = geacps_annex.0
	title = geacps_annex.0.t
	desc = geacps_annex.0.d
	#picture = 
	is_triggered_only = yes
	option = {
		name = geacps_annex.0.a
		ai_chance = {	base = 10	}
		every_state = {
			limit = {
				is_core_of = AFG
				is_fully_controlled_by = ROOT
				NOT = {
					is_core_of = ROOT
					is_claimed_by = ROOT
				}
			}
			if = {
				limit = {	NOT = {	country_exists = AFG	}	}
				AFG = {	transfer_state = PREV	}
			}
			else = {
				create_dynamic_country = {
					original_tag = AFG
					transfer_state = PREV
				}
			}
		}
		puppet = AFG
	}
	option = {
		name = geacps_annex.0.b
		ai_chance = {	base = 0	}
		AFG = {	add_ideas = colonial_government	}
		every_state = {
			limit = {
				is_core_of = AFG
				is_fully_controlled_by = ROOT
				NOT = {
					is_core_of = ROOT
					is_claimed_by = ROOT
				}
			}
			AFG = { transfer_state = PREV }
		}
		puppet = AFG
	}
}